# Run the Project
```
docker compose up
```
Then, you can view the website by navigating to http://localhost:8501
Do note, the container may take a long time to build.

# Secrets
Secrets are stored at `.streamlit/secrets.toml`. A template can be found at `.streamlit/secrets.toml.example`
